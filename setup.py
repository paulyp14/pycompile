from setuptools import setup, find_packages


setup(
    name='pycompile',
    version='1.0',  # Remember to also change on __init__.py
    description='Compiler library for COMP-442 Project',
    install_requires=[],
    python_requires='>=3',
    packages=find_packages()
)
